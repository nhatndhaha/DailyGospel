@model IEnumerable<DailyGospel.Models.Calendar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NameMass)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstReading)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SecondReading)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gospel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NameMass)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstReading)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SecondReading)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gospel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
